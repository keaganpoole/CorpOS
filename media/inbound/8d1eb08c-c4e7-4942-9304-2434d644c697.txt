Here is your **fully upgraded, production-grade prompt** with the menu system integrated cleanly and zero ambiguity:

---

You are implementing a **new feature** inside the existing Variables Panel. This is **not a redesign**. You are only adding a new capability with tightly scoped behavior.

---

## 🎯 Objective

Add support for viewing **variables from previous nodes in the current scenario builder flow**, allowing users to pass data forward between nodes.

This requires introducing a **minimal, scalable menu system** inside the Variables Panel to support multiple sections (future tabs).

---

## 🧭 Variables Panel Menu (NEW)

There are currently **no tabs**. You must introduce a **minimal, reusable menu system** at the top of the Variables Panel.

### The menu must support switching between:

1. **Current Variables** (existing behavior — default)
2. **Previous Node Variables** (new feature)

---

## 🎨 Menu UI Constraints (CRITICAL)

* The menu must be:

  * Minimalistic, sleek, and clean
  * Visually consistent with the existing panel
  * Small and non-intrusive

* It must:

  * Sit naturally at the top of the Variables Panel
  * Not shift or break existing layout
  * Feel like it was always part of the UI

---

## 🧩 Menu Implementation Rules

* Implement as a **simple, scalable structure** (not hardcoded logic)

Example structure:

```ts
const variablePanelSections = [
  { id: "current", label: "Current" },
  { id: "previous_nodes", label: "Previous" },
]
```

* The system must:

  * Allow adding future sections by adding entries to this config
  * Not require layout rewrites when new sections are added

---

## 🚫 Menu Constraints

* Do NOT:

  * Redesign the panel
  * Add heavy navigation UI
  * Introduce new visual styles
  * Create modals, dropdowns, or popovers

* Do:

  * Keep it lightweight (simple tab-like toggle)
  * Reuse existing typography, spacing, and interaction patterns

---

## 📌 Core Behavior (Previous Node Variables)

* The **Previous Node Variables** section must:

  * Show variables from **all previous nodes only**

    * Exclude current node
    * Exclude future nodes

  * Display nodes in this order:

    * **Most recent previous node at the top**
    * Earliest node at the bottom

  * Within each node:

    * Show variables exactly like existing variable sets UI

---

## 📌 Example Flow

* Trigger: Appointment Confirmed
* Node 2: Update Records
* Node 3: Create Record (current node)

👉 In Node 3 → "Previous" section should show:

* Update Records variables (top)
* Appointment Confirmed variables (below)

---

## 🎨 Variable Display Constraints (CRITICAL)

* **DO NOT redesign variable UI**
* The variables must look and behave **identical to existing variable sets**

### Implementation Strategy:

* **Duplicate the existing variable set component**
* Reuse it inside the "Previous" section
* Do NOT rebuild from scratch

---

## ⚙️ Data Logic

* Replace the data source in the duplicated component:

  * Instead of global/table variables → fetch **variables from previous nodes**

* You must:

  * Traverse the current scenario graph
  * Identify all nodes before the current node
  * Extract their variables
  * Group variables by node
  * Maintain correct ordering (most recent → oldest)

---

## 🚫 Strict Constraints

* Do not modify:

  * Existing variable panel behavior
  * Existing variable sets
  * Any unrelated components

* Do not:

  * Introduce new UI patterns
  * Change interaction behavior
  * Add modals, popups, or redesign layouts

* If your implementation changes the visual appearance of the Variables Panel beyond adding a minimal menu and one additional section, it is incorrect.

---

## ❓ Clarification Protocol (MANDATORY)

* If anything is unclear or ambiguous, **ask targeted questions before coding**

* Only ask questions if they block correctness

* Do not ask trivial questions

* Prefer reasonable assumptions when safe

* Before implementing, briefly restate your understanding of the task in 1–2 sentences

---

## ✅ Definition of Done

* A minimal menu exists in the Variables Panel
* "Current" section preserves existing behavior exactly
* "Previous" section displays previous node variables correctly
* Variables are grouped and ordered properly
* UI is identical to existing variable sets
* Users can select variables exactly the same way
* System supports adding future sections cleanly
* No regressions to current functionality

---

If something is unclear, inspect the existing variable set implementation and mirror it exactly before modifying data logic.
